{
  "name": "escapeland",
  "version": "0.3.0",
  "private": true,
  "workspaces": [
    "app",
    "studio"
  ],
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.0.0"
  },
  "scripts": {
    "dev": "bun --filter '*' dev",
    "format": "bunx prettier --write ."
  }
}

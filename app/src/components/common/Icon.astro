---
import { Icon as IconInternal } from 'astro-icon/components'

interface Props {
  name: string
  size?: number
  class?: string
}

const { name, size = 16, ...rest } = Astro.props
---

<IconInternal name={`lucide:${name}`} size={size} {...rest} />
